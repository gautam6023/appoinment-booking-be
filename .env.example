# MongoDB Connection String
# Format: mongodb://[username:password@]host[:port][/database][?options]
# For local MongoDB: mongodb://localhost:27017/appointments
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/appointments
MONGODB_URI=mongodb://localhost:27017/appointments

# JWT Secret Key
# IMPORTANT: Change this to a secure random string in production
# You can generate one using: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production

# Server Port
PORT=3001

# Frontend URL for CORS
# This should match your frontend URL
FRONTEND_URL=http://localhost:5173

# Node Environment
NODE_ENV=development

# Server Timezone (optional)
# Used for generating time slots (9 AM - 5 PM)
# Defaults to UTC if not specified
SERVER_TIMEZONE=UTC